'use strict';
const EARTH_RADIUS_M=6371000;const CATEGORY_DEFS={company:['office','craft'],tech:['office','craft'],industry:['industrial','craft'],finance:['office','amenity'],public:['amenity'],health:['amenity','healthcare'],education:['amenity'],retail:['shop'],hospitality:['tourism']};const TECHNICAL_WORDS=['software','informatik','engineering','ingenieur','maschinenbau','industrie','industrial','manufacturer','automation','data','artificial intelligence','ai','logistik','logistics','energy','energie','technology','technologie','elektronik','robotik'];const BUSINESS_WORDS=['consulting','beratung','bank','finance','steuerberater','accounting','law','rechtsanwalt','versicherung','insurance','office','unternehmen','firma','gmbh','audit','marketing','agentur'];const PUBLIC_WORDS=['hospital','krankenhaus','clinic','klinik','school','schule','university','hochschule','college','public','stadt','government','ngo','association','verein','forschung','research','laboratory','labor'];const OPPORTUNITY_WORDS=['karriere','career','jobs','stellenangebote','praktikum','internship','werkstudent','trainee','ausbildung','duales studium','graduate'];function clamp(n,min,max){n=Number(n);return Number.isFinite(n)?Math.max(min,Math.min(max,n)):min}function normalizeText(v){return String(v||'').normalize('NFKD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim()}function haversineM(lat1,lng1,lat2,lng2){const p1=Number(lat1)*Math.PI/180,p2=Number(lat2)*Math.PI/180,dp=(Number(lat2)-Number(lat1))*Math.PI/180,dl=(Number(lng2)-Number(lng1))*Math.PI/180;const a=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return 2*EARTH_RADIUS_M*Math.atan2(Math.sqrt(a),Math.sqrt(1-a))}function firstTag(t,n){for(const k of n)if(t&&t[k])return String(t[k]).trim();return ''}function categoryFromTags(t={}){for(const k of ['office','craft','industrial','amenity','shop','tourism','healthcare'])if(t[k])return String(t[k]).replace(/_/g,' ');return firstTag(t,['brand','operator'])||'organisation'}function addressFromTags(t={}){const a=[t['addr:housenumber'],t['addr:street']].filter(Boolean).join(' '),b=[t['addr:postcode'],t['addr:city']||t['addr:town']||t['addr:village']].filter(Boolean).join(' ');return[a,b].filter(Boolean).join(', ')}function priorityForScore(s){return s>=85?'A+':s>=70?'A':s>=55?'B':s>=40?'C':'D'}function scoreEntity(e,center={},keywords=[]){const text=normalizeText([e.name,e.category,e.address,e.website,e.detectedSignals,...(e.types||[])].filter(Boolean).join(' '));let score=15;const reasons=[],signals=[];const th=TECHNICAL_WORDS.filter(w=>text.includes(normalizeText(w))),bh=BUSINESS_WORDS.filter(w=>text.includes(normalizeText(w))),ph=PUBLIC_WORDS.filter(w=>text.includes(normalizeText(w))),oh=OPPORTUNITY_WORDS.filter(w=>text.includes(normalizeText(w)));if(th.length){score+=27;reasons.push('secteur technique/industrie');signals.push(...th.slice(0,3))}else if(bh.length){score+=22;reasons.push('services professionnels/finance');signals.push(...bh.slice(0,3))}else if(ph.length){score+=18;reasons.push('institution/recherche/santé');signals.push(...ph.slice(0,3))}const kh=(keywords||[]).map(normalizeText).filter(Boolean).filter(k=>text.includes(k));if(kh.length){score+=Math.min(20,8+kh.length*4);reasons.push('mots-clés correspondants');signals.push(...kh.slice(0,4))}if(oh.length){score+=20;reasons.push('signaux carrière');signals.push(...oh.slice(0,4))}if(e.website){score+=8;reasons.push('site web')}if(e.phone)score+=5;if(e.email)score+=7;let d=Number(e.distanceFromCenterM);if(!Number.isFinite(d)&&Number.isFinite(Number(e.lat))&&Number.isFinite(Number(e.lng)))d=Math.round(haversineM(center.lat,center.lng,e.lat,e.lng));if(Number.isFinite(d)){if(d<=1000)score+=12;else if(d<=3000)score+=9;else if(d<=7000)score+=5}score=Math.round(clamp(score,0,100));return{...e,distanceFromCenterM:Number.isFinite(d)?Math.round(d):null,opportunityScore:score,priority:priorityForScore(score),opportunityReason:reasons.join(' • ')||'profil à qualifier',detectedSignals:[...new Set([...(String(e.detectedSignals||'').split(',').map(x=>x.trim()).filter(Boolean)),...signals])].join(', ')}}function parseOverpass(payload,center){const out=[];for(const el of(payload&&payload.elements)||[]){const t=el.tags||{},lat=Number(el.lat??el.center?.lat),lng=Number(el.lon??el.center?.lon);if(!Number.isFinite(lat)||!Number.isFinite(lng))continue;const name=firstTag(t,['name','brand','operator']);if(!name)continue;out.push({source:'OpenStreetMap',sourceId:`osm:${el.type}:${el.id}`,name,category:categoryFromTags(t),address:addressFromTags(t),city:firstTag(t,['addr:city','addr:town','addr:village']),country:firstTag(t,['addr:country']),lat,lng,distanceFromCenterM:Math.round(haversineM(center.lat,center.lng,lat,lng)),phone:firstTag(t,['contact:phone','phone','contact:mobile']),website:firstTag(t,['contact:website','website','url']),email:firstTag(t,['contact:email','email']),sourceUrl:`https://www.openstreetmap.org/${el.type}/${el.id}`,googleMapsUrl:`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(`${lat},${lng}`)}`,types:Object.entries(t).filter(([k])=>['office','craft','industrial','amenity','shop','tourism','healthcare'].includes(k)).map(([,v])=>String(v))})}return out}function mapGooglePlace(p,center){const lat=Number(p?.location?.latitude),lng=Number(p?.location?.longitude),name=p?.displayName?.text||'';if(!name||!Number.isFinite(lat)||!Number.isFinite(lng))return null;return{source:'Google Places',sourceId:`google:${p.id||''}`,placeId:p.id||'',name,category:String(p.primaryType||(p.types&&p.types[0])||'organisation').replace(/_/g,' '),address:p.formattedAddress||'',lat,lng,distanceFromCenterM:Math.round(haversineM(center.lat,center.lng,lat,lng)),phone:p.nationalPhoneNumber||p.internationalPhoneNumber||'',website:p.websiteUri||'',email:'',sourceUrl:p.googleMapsUri||'',googleMapsUrl:p.googleMapsUri||'',rating:Number(p.rating)||null,userRatingCount:Number(p.userRatingCount)||0,types:Array.isArray(p.types)?p.types:[]}}function canonicalKey(e){const n=normalizeText(e.name),lat=Number(e.lat),lng=Number(e.lng);if(n&&Number.isFinite(lat)&&Number.isFinite(lng))return`${n}|${lat.toFixed(4)}|${lng.toFixed(4)}`;if(e.placeId)return`place:${e.placeId}`;return`${n}|${normalizeText(e.address)}`}function mergeEntity(a,b){const r={...a};for(const f of['category','address','city','country','phone','website','email','sourceUrl','googleMapsUrl','placeId'])if(!r[f]&&b[f])r[f]=b[f];r.source=[...new Set([...(String(a.source||'').split(' + ')),...(String(b.source||'').split(' + '))].filter(Boolean))].join(' + ');r.types=[...new Set([...(a.types||[]),...(b.types||[])])];return r}function dedupeEntities(xs){const m=new Map();for(const e of xs||[]){if(!e?.name)continue;const k=canonicalKey(e);m.set(k,m.has(k)?mergeEntity(m.get(k),e):{...e})}return[...m.values()]}function sanitizeKeywords(v,max=10){const a=Array.isArray(v)?v:String(v||'').split(/[\n,;]+/);return[...new Set(a.map(x=>String(x).trim()).filter(x=>x.length>=2&&x.length<=80))].slice(0,max)}function sanitizeCategories(v){const a=Array.isArray(v)?v:[],valid=Object.keys(CATEGORY_DEFS),out=[...new Set(a.filter(x=>valid.includes(x)))];return out.length?out:['company','tech','industry','finance','public','health','education']}function buildOverpassQuery(lat,lng,radiusM,categories=[]){const r=Math.round(clamp(radiusM,250,25000)),a=Number(lat).toFixed(6),o=Number(lng).toFixed(6),around=`(around:${r},${a},${o})`,c=sanitizeCategories(categories),q=[];if(c.includes('company')||c.includes('tech'))q.push(`nwr["name"]["office"]${around};`,`nwr["name"]["craft"]${around};`);if(c.includes('industry'))q.push(`nwr["name"]["industrial"]${around};`);if(c.includes('finance'))q.push(`nwr["name"]["amenity"~"bank|bureau_de_change"]${around};`);if(c.includes('public'))q.push(`nwr["name"]["amenity"~"townhall|courthouse|police|fire_station|research_institute|library"]${around};`);if(c.includes('health'))q.push(`nwr["name"]["amenity"~"clinic|hospital|doctors|pharmacy"]${around};`,`nwr["name"]["healthcare"]${around};`);if(c.includes('education'))q.push(`nwr["name"]["amenity"~"school|university|college|kindergarten"]${around};`);if(c.includes('retail'))q.push(`nwr["name"]["shop"]${around};`);if(c.includes('hospitality'))q.push(`nwr["name"]["tourism"~"hotel|hostel|guest_house"]${around};`);return`[out:json][timeout:25][maxsize:60000000];\n(\n${q.join('\n')}\n);\nout center tags qt;`}function sanitizeSearchInput(i={}){const lat=Number(i.lat),lng=Number(i.lng);if(!Number.isFinite(lat)||lat<-90||lat>90)throw new Error('Latitude invalide');if(!Number.isFinite(lng)||lng<-180||lng>180)throw new Error('Longitude invalide');return{lat,lng,radiusM:Math.round(clamp(i.radiusM||3000,250,25000)),keywords:sanitizeKeywords(i.keywords),categories:sanitizeCategories(i.categories),minScore:Math.round(clamp(i.minScore||0,0,100)),maxResults:Math.round(clamp(i.maxResults||1500,50,2500)),useGoogle:i.useGoogle===true}}function rankEntities(xs,center,keywords,minScore=0,max=1500){return dedupeEntities(xs).map(e=>scoreEntity(e,center,keywords)).filter(e=>e.opportunityScore>=minScore).sort((a,b)=>b.opportunityScore-a.opportunityScore||(a.distanceFromCenterM??1e12)-(b.distanceFromCenterM??1e12)).slice(0,max)}module.exports={clamp,normalizeText,haversineM,scoreEntity,parseOverpass,mapGooglePlace,dedupeEntities,buildOverpassQuery,sanitizeKeywords,sanitizeCategories,sanitizeSearchInput,rankEntities};